apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-large
spec:
  replicas: 20

  selector:
    matchLabels:
      app: nginx

  template:
    metadata:
      labels:
        app: nginx

    spec:
      containers:
        - name: nginx
          image: nginx